{
  "name": "mi-asistente",
  "version": "0.9.0-beta",
  "private": true,
  "description": "PWA de gestión de tareas con backend SQLite",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "check": "node --check app.js && node --check server/ai.js && node --check server/database.js && node --check server/server.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "multer": "^2.2.0",
    "sqlite3": "^5.1.7"
  }
}
